passwd: Unix user and groups database support

[[unix::passwd]] provides functions to assist in parsing Unix /etc/passwd and
/etc/group files, for working with the local user and group system databases.
